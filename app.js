const heading = React.createElement('h1',{},"hello im learning react js"); // return an element 
// we can make nested create element too

const root = ReactDOM.createRoot(document.getElementById('root'));  


root.render(heading);